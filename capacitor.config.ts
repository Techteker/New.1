import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = { appId: 'com.proximax.homeworkout', appName: 'Home Workout Pro', webDir: 'www', bundledWebRuntime: false };
export default config;
